name: ecom-support-kit

services:
  postgres:
    # PostgreSQL 18.6 on the reviewed Wolfi image. The digest is the complete
    # immutable input; there is no floating tag in this deployment manifest.
    image: cgr.dev/chainguard/postgres@sha256:13d374b74c78215f3b902435ebd96c4521245d53fdb69e8f166fdcebb155f219
    restart: unless-stopped
    environment:
      POSTGRES_DB: support
      POSTGRES_USER: support
      # Local dev default only. For any real deployment, set POSTGRES_PASSWORD
      # in your shell environment. Never commit a real password.
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-support-local-only}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./sql/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U support -d support"]
      interval: 5s
      timeout: 5s
      retries: 10

  service:
    build: ./service
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      # Offline defaults. Mount config.json to override with real values.
      SERVICE_HOST: "0.0.0.0"
      STORE_DRIVER: postgres
      PGHOST: postgres
      PGPORT: "5432"
      PGDATABASE: support
      PGUSER: support
      PGPASSWORD: ${POSTGRES_PASSWORD:-support-local-only}
      LLM_DRIVER: mock
      PORT: "8080"
    # Real secrets stay out of git. Mount a chmod 600 config.json when you need
    # a live store or a live model provider:
    # volumes:
    #   - ./config.json:/app/config.json:ro
    ports:
      - "127.0.0.1:${SUPPORT_PORT:-8080}:8080"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8080/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
